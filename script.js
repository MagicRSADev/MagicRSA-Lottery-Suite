function analyzeDraw(){let n=document.getElementById('numbers').value.trim().split(/[\s,]+/).map(Number).filter(x=>!isNaN(x));let u=[...new Set(n)],bad=n.filter(x=>x<1||x>80);if(n.length!=20||u.length!=20||bad.length){document.getElementById('results').innerHTML='Validation failed';return;}let a=MASTER_WHEEL.map((g,i)=>({g:i+1,m:g.filter(v=>u.includes(v)).length})).sort((x,y)=>y.m-x.m);let h='<table border=1><tr><th>Rank</th><th>Group</th><th>Matches</th></tr>';a.slice(0,5).forEach((r,i)=>h+='<tr><td>'+(i+1)+'</td><td>'+r.g+'</td><td>'+r.m+'/7</td></tr>');document.getElementById('results').innerHTML=h+'</table>';}